# AFD 증분 버그 감사 리포트 (inc2)

- **감사일**: 2026-04-01
- **대상 커밋**: `508c15e` (main)
- **이전 태그**: `BUG-AUDIT-2026-04-01-inc1`
- **감사 범위**: 변경 파일 9개 (server.ts, http-routes.ts, mcp-handler.ts, workspace-map.ts, types.ts, cli.ts, watch.ts, version.ts, mcp-protocol.test.ts)
- **모드**: `--fix`

---

## 요약

| 심각도 | 발견 | 수정 | 미수정 |
|--------|------|------|--------|
| CRITICAL | 0 | — | — |
| HIGH | 0 | — | — |
| MEDIUM | 4 | 4 | 0 |
| LOW | 2 | 2 | 0 |
| **합계** | **6** | **6** | **0** |

---

## MEDIUM (4건)

### M-35. workspace-map 타이머 cleanup 미연결
- **위치**: `src/daemon/server.ts:60,67` + `src/daemon/workspace-map.ts:83-86`
- **분류**: 리소스 누수
- **설명**: `_cleanupResources.mapTimer`가 선언만 되고 `wsMap`의 내부 타이머가 할당되지 않아 데몬 종료 시 타이머 미정리.
- **수정**: `wsMapGetTimer` 함수 참조를 cleanup에 등록, 종료 시 `getTimer()` 호출하여 정리.
- **상태**: 수정완료

### M-36. `assertInsideWorkspace` 중복 + `process.cwd()` 사용
- **위치**: `src/daemon/http-routes.ts:16-21`, `src/daemon/mcp-handler.ts:66-71`
- **분류**: 보안 / 유지보수
- **설명**: 동일 함수가 2곳에 복붙. `process.cwd()` 대신 워크스페이스 루트(`ctx.ws.root`)를 사용해야 함.
- **수정**: `src/daemon/guards.ts` 공통 모듈 추출. 호출부에서 `_assertWs(absPath, ctx.ws.root)` 직접 호출.
- **상태**: 수정완료

### M-37. `/sync` 엔드포인트 `JSON.parse` 미보호
- **위치**: `src/daemon/http-routes.ts:201`
- **분류**: 크래시 / 견고성
- **설명**: DB의 `patch_op` 파싱 실패 시 `/sync` 핸들러 전체 크래시 (500 응답).
- **수정**: try-catch 추가, 손상된 행은 `flatMap`으로 건너뜀.
- **상태**: 수정완료

### INC2-01. `version.ts` — `readFileSync`/`JSON.parse` 미보호
- **위치**: `src/version.ts:4-7`
- **분류**: 에러 처리 / UX
- **설명**: `package.json` 부재 시 CLI 전체 크래시. 번들 바이너리에서 경로 불일치 가능.
- **수정**: try-catch + `"0.0.0-unknown"` 폴백. `pkg.version` 타입 검증.
- **상태**: 수정완료

---

## LOW (2건)

### L-23. `createSeamLogger` — `state.sseClients` 직접 참조
- **위치**: `src/daemon/server.ts:80-98`
- **분류**: 유지보수 / 결합도
- **설명**: 모듈 레벨 `state`를 클로저로 직접 참조. 테스트 어려움.
- **상태**: 미수정 (설계 변경 범위 — 현재 기능에 영향 없음)

### L-24. workspace-map root files `statSync` → `lstatSync`
- **위치**: `src/daemon/workspace-map.ts:58`
- **분류**: 일관성
- **설명**: `walk()`는 `lstatSync` 사용하나 루트 파일 섹션만 `statSync`. 심링크 추적.
- **수정**: `lstatSync`로 통일.
- **상태**: 수정완료

---

## 버그 완료 태그

> **`BUG-AUDIT-2026-04-01-inc2`**
